.icon {
    display: inline-block;
    vertical-align: middle;
}

{{#sprites}}
.icon-{{name}} {
    width: {{px.width}};
    height: {{px.height}};
    background: url({{escaped_image}}) {{px.offset_x}} {{px.offset_y}};
}
{{/sprites}}